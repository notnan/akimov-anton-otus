{
  "name": "home-work-2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node build/index.js",
    "build": "rimraf ./build ./numbers && tsc"
  },
  "author": "Anton Akimov",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.0.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.0.0"
  },
  "dependencies": {
    "@tsmx/human-readable": "^1.0.6",
    "config": "^3.3.6",
    "convert-size": "^1.1.5",
    "to-arraybuffer": "^1.0.1"
  }
}
